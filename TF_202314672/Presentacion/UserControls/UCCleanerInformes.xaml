<UserControl x:Class="Presentacion.UserControls.UCCleanerInformes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentacion.UserControls"
             xmlns:wf="clr-namespace:System.Windows.Forms.DataVisualization.Charting;assembly=System.Windows.Forms.DataVisualization"
             xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
             mc:Ignorable="d" 
             d:DesignHeight="660" d:DesignWidth="1000" Background="White" Loaded="loaded">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Informes de Reservas" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="#7FE9DE" CornerRadius="10" Padding="30" Margin="100,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Total de Reservas realizadas" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="tbTotalReservasRealizadas" Text="0" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Total de Reservas realizadas en el Mes actual" FontSize="20" FontWeight="SemiBold" Margin="0,10,0,0"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="tbTotalReservasRealizadasMesActual" Text="0" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,10,0,0"/>
            </Grid>
        </Border>

        <TextBlock Grid.Row="2" Text="Reservas por tipo:" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="3" Margin="100,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Width="380" Height="330">
                <wfi:WindowsFormsHost>
                    <wf:Chart x:Name="pieChartReservas"/>
                </wfi:WindowsFormsHost>
            </Grid>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0">
                <TextBlock Text="Sueldo :" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock x:Name="tbSueldo" Text="S/ 0.00" FontSize="32" FontWeight="Bold" Foreground="DarkSlateGray"/>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
